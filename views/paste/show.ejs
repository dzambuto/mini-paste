<!DOCTYPE html>
<html lang="en">
  <% include ../layout/head %>
  <body>
    
    <% include ../layout/navbar %>
    
    <div class="container">
      
      <h3><i class="fa fa-tag"></i> <%= paste.title %></h3>
      <p><strong>Created</strong>: <%= paste.createdAt.getDate() %>/<%= paste.createdAt.getMonth()+1 %>/<%= paste.createdAt.getFullYear() %> | <strong>Hits</strong>: <%= paste.hits %></p>
      <p>
        <button type="button" class="btn btn-default" id="copy" data-clipboard-target="contentToCopy"><i class="fa fa-files-o"></i> Copy</button>
        <a class="btn btn-default" href="/?id=<%= paste.hid %>" role="button"><i class="fa fa-code-fork"></i> New Version</a>
      </p>
      <p><pre><code id="contentToCopy"><%= paste.content %></code></pre></p>
      
      <h4>Raw Paste</h4>
      <textarea class="form-control" rows="10"><%= paste.content %></textarea>
      
    </div>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
    <script src="//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/highlight.js/8.0/highlight.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/zeroclipboard/1.3.3/ZeroClipboard.min.js"></script>
    <script>
    hljs.initHighlightingOnLoad();
    $(document).ready(function(){
      ZeroClipboard.config( { moviePath: '//cdnjs.cloudflare.com/ajax/libs/zeroclipboard/1.3.3/ZeroClipboard.swf' } );
      var client = new ZeroClipboard( document.getElementById("copy") );
    });
    </script>
  </body>
</html>